import_tracking/
├── __init__.py                # Initialise le package Python et importe le dossier models
├── __manifest__.py           # Métadonnées du module (nom, dépendances, fichiers XML)
├── data/
│   └── sequence_data.xml      # (Optionnel) Pour générer des numéros de suivi auto (ex: IMP/2026/001)
├── models/
│   ├── __init__.py            # Importe les fichiers Python du dossier
│   └── import_tracking.py     # Définition de la classe et des champs (le cœur de l'addon)
├── security/
│   ├── ir.model.access.csv    # Définit qui peut lire/écrire/modifier les données
│   └── security_groups.xml    # (Optionnel) Pour créer des groupes "Utilisateur" vs "Gestionnaire"
├── views/
│   ├── import_tracking_views.xml  # Définition des vues (Formulaire, Liste, Kanban, Recherche)
│   └── menus.xml              # Définition de la structure des menus dans l'interface Odoo
└── static/
    └── description/
        └── icon.png           # L'icône qui apparaîtra dans le tableau de bord Odoo